<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bitcoinstats">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".BlockDetailActivity"></activity>
        <activity android:name=".LastBlocksActivity" />
        <activity android:name=".BlockchainInfoActivity" />
        <activity android:name=".TxDetailActivity" />
        <activity android:name=".MempoolActivity" />
        <activity android:name=".TotalTxFeesActivity" />
        <activity android:name=".TxPerDayActivity" />
        <activity android:name=".MarketPriceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>